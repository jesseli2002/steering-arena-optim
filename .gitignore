.claude
.env
.vscode

__pycache__

wandb

# Artefacts on big model
data
# Artefacts for smoke-test model (only locally listed)
data_local

# Regenerable caches (activations, etc.) -- machine-local, never committed
.cache

# Machine-local agent tooling (hardcoded paths, personal tokens) -- never committed
local_scripts
